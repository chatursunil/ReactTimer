const React = require('react');

const Countdown = React.createClass({
    render: () => {
        return (
            <div>
                <p>Countdown.jsx component rendered</p>
            </div>
        );
    }
});

module.exports = Countdown;